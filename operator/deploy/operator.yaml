# Generated by IBM TransformationAdvisor
# Fri Aug 21 04:34:32 UTC 2020
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopieasy-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: shopieasy-operator
  template:
    metadata:
      labels:
        name: shopieasy-operator
    spec:
      serviceAccountName: shopieasy-operator
      containers:
        - name: shopieasy-operator
          image: openliberty/operator:0.0.1
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "shopieasy-operator"
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                - amd64
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 3
            preference:
              matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                - amd64
